package ru.safiullina.HW_Test_containers.profile;

/**
 * Интерфейс, реализации которого мы будем вызывать в зависимости от параметров вашего приложения.
 */
public interface SystemProfile {
    String getProfile();
}
